/**
 * Active brand. Switch with `node scripts/brand.mjs v3|v2|c4`, which also swaps
 * every icon reference (manifest, index.html, service worker, notifications).
 */
export const BRAND: 'v3' | 'v2' | 'c4' = 'c4';
